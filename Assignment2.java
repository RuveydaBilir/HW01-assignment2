class Assignment2{
  //method a

  //method b

  //method c
  public static int findMin(int[] arr){
    int minNum;
    minNum = arr[0];
    for (int i = 0; i < arr.length; i++) {
      if (minNum < arr[i]) {
        minNum = arr[i];
      } 
    }
  }

  public static int findMax(int[] arr){
    int maxNum;
    maxNum = arr[0];
    for (int i = 0; i < arr.length; i++) {
      if (maxNum < arr[i]) {
        maxNum = arr[i];
      } 
    }
  }
  //method d

  //method e
  
}
